# Environment Variables - Copy to .env (never commit .env)

# Development Database
DB_PASSWORD_DEV=your_dev_db_password

# Production Database
DB_PASSWORD=your_prod_db_password

# Optional: Internet Archive
IA_API_KEY=optional_ia_key_if_needed

# Optional: MediaWiki
WIKI_API_PASSWORD=mediawiki_api_password
